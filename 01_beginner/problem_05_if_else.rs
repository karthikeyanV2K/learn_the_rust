// Problem 5: If-Else Logic
//
// Learn: Conditionals, boolean logic
//
// TASK: Classify a number (positive/negative/zero) and even/odd

fn main() {
    let number = 42;
    
    // TODO: Use if-else to classify number as positive/negative/zero
    // TODO: Use if as an expression to determine "even" or "odd"
    // Hint: if number % 2 == 0 { "even" } else { "odd" }
    
    // BONUS CHALLENGES:
    // 1. Try different numbers: 0, -5, 100
    // 2. Nest if statements: classify into 4 quadrants using (x, y)
    // 3. Use logical operators: && (and), || (or), ! (not)
    //    Example: if x > 0 && x < 10 { ... }
}

/*
Expected Output (for number = 42):
42 is positive
42 is even
*/
